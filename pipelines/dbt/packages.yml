packages:
  # dbt utility macros (generate_surrogate_key, safe_divide, etc.)
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]

  # ClickHouse adapter for fact table writes
  - package: ClickHouse/dbt-clickhouse
    version: [">=1.7.0", "<2.0.0"]

  # Data quality tests beyond dbt core (expression_is_true, mutually_exclusive_ranges)
  - package: calogica/dbt_expectations
    version: [">=0.10.0", "<1.0.0"]
